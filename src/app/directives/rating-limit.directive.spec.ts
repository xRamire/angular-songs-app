import { RatingLimitDirective } from './rating-limit.directive';

describe('RatingLimitDirective', () => {
  it('should create an instance', () => {
    const directive = new RatingLimitDirective();
    expect(directive).toBeTruthy();
  });
});
